# Chore Club

A dependency-free weekly chores matrix made with plain HTML, CSS, and JavaScript.

Open `index.html` directly to use it, or serve this folder with any static file server to enable offline caching after the first visit.

Family settings, weekly checkoffs, and rewards are saved in the browser with `localStorage`.
